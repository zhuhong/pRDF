#ifndef STRING_OPERATE_H
#define STRING_OPERATE_H

#include <string>
#include <vector>
#include <stack>
#include <iostream>

using namespace std;

string Split(string s,char a,int n);
vector<string> Split_v(string s,char a);

#endif
